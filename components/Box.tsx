import styled from 'styled-components'
import {background, border, space, color, width, layout, flexbox, position, typography} from 'styled-system'

const Box = styled('div')`
  ${background} ${border} ${space} ${color} ${width} ${layout} ${flexbox} ${position} ${typography};
`

export default Box
